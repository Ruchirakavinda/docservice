server:
  port: 8081
  error:
    include-binding-errors: always
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/docservice
    password: rchrkvnd
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true
